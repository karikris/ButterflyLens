# ButterflyLens Build Week Delta

This is an append-oriented summary of work created after the baseline in [`BUILD_WEEK_BASELINE.md`](BUILD_WEEK_BASELINE.md). It must never count imported upstream capability as new ButterflyLens work.

## Current delta

As of `2026-07-17T13:28:43Z`, the repository contains only Phase 0 initialization and provenance work:

- a public repository on `main` with the required title, tagline, scope caveat, and status language;
- `.gitignore`, including the user-directed `AGENTS.md` exclusion;
- the immutable repository/tool/upstream baseline;
- initialized GitHits, commit, model-usage, and review-attestation provenance.

## Explicitly not yet implemented

No licence decision, software/data rights audit, application code, taxonomy pack, ALA snapshot, Flickr call, downloaded image, model execution, community review, quality estimate, map, worker, deployment, OpenAI runtime integration, export, video, deck, or competition metric is included in this delta.

## Delta accounting rule

Each later entry must identify:

1. the ButterflyLens commit;
2. whether the work is new or adapted;
3. any origin repository, immutable SHA, and source path;
4. targeted verification performed;
5. human decisions or review evidence;
6. task push and remote `main` SHA.

