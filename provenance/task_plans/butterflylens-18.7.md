# Task 18.7 plan — align the demonstration packet with the submitted map

Task ID: `butterflylens-18.7`

Objective: replace the current recording instructions with a versioned second
production packet tied to the immutable rights-screened Submitted ALA map,
while preserving the historical v1 packet and retaining the honest
not-recorded, not-reviewed, and not-uploaded status.

Competition criteria improved: 93 and 94, public demonstration video and
sub-three-minute working-product coverage. This task makes the final recording
accurate and reproducible; it does not satisfy either criterion until the human
narration, final-cut review, and public YouTube publication occur.

Starting and remote SHA:
`27a37f078006436d387b3e07242ca5a29d84668e`.

Capture source SHA:
`45fb5ac07dcd51852c9e92217667f3f5052868fe`, the pushed Task 18.4 map,
analyst, README, and judge-route reconciliation boundary.

BioMiner boundary: this task does not overlap BioMiner's active Flickr metadata
fetch. No BioMiner worktree, mutable record, partial output, or Flickr API will
be inspected, copied, counted, or called.

Agent files read: root `AGENTS.md`, `docs/agents/TOOLS.md`,
`GIT_AND_PROVENANCE.md`, `SCIENCE_AND_DATA.md`, `ARCHITECTURE.md`,
`TESTING_AND_RELEASE.md`, and `TASK_TEMPLATE.md`.

Skill used: Headroom to inspect the exact source goal under receipt
`898dbe5ec3520d1425bf5d0f`.

GitHits: unavailable and disabled by direct user instruction for the rest of
the goal. No call will be made; the exact disabled status will be recorded.

Valyu: not needed. All recording claims, paths, counts, fingerprints, and state
boundaries derive from fixed local Git objects and committed Submitted
artifacts. No current provider term, API behavior, or external fact changes.

## Subtask 18.7.1 — publish a map-aligned v2 production packet

- Preserve the v1 manifest and captions byte-for-byte as historical pre-map
  evidence.
- Pin the v2 packet to the Task 18.4 capture source, canonical Submitted
  snapshot, submitted map snapshot, and exact evidence-source checksums.
- Replace the stale withheld-layer and no-selectable-cell instructions with the
  real 213,310-row, 630-cell rights-screened map and exact H3
  `838c23fffffffff` drilldown.
- Keep Flickr unavailable, partial BioMiner output excluded, the M5 heartbeat
  unavailable, YOLOE/BioCLIP unfinished, local review unsubmitted, precision
  unavailable, live Bounded model not invoked, and occurrence export blocked.
- Use the map-grounded stored analyst replay that cites the ALA aggregate and
  refuses an unsupported Flickr difference.
- Keep the target runtime at 2:48 with 2:40 of working-product footage.
- Validate every measured number against pinned artifacts, every capture source
  against the pinned Git commit, caption continuity and pace, historical packet
  immutability, and the final publication boundary.

Expected files: `DEMO_VIDEO.md`, v2 JSON/SRT assets, video validator tests,
README wording, and provenance ledgers.

Contracts affected: demonstration production packet only. Submitted data,
public UI, source snapshots, analyst replay, scientific release policy, and the
historical v1 video packet remain unchanged.

Rights/privacy: record only the local submitted product, its rights-cleared
review fixture, aggregate geometry, and coordinate-free evidence sample. Block
all non-local browser traffic. Do not show active Flickr output, credentials,
private worker observations, reviewer identities, or sensitive coordinates.

Scientific risk: the map shows selected ALA baseline evidence coverage, not
complete ground truth, presence, absence, abundance, identity, cross-source
impact, or a released occurrence. A production packet is not a completed video.

Tests:

- v2 JSON parsing and focused video validator;
- exact source-commit and evidence checksum verification;
- measured-claim derivation from submitted map, quality, and analyst artifacts;
- caption continuity, pace, runtime, and product-footage checks;
- historical v1 packet checksum preservation and stale-claim rejection;
- README completion-boundary check, full repository gate, rights, licensing,
  release security, current completion audit, JSON/JSONL parsing, Python
  compilation, and `git diff --check`.

Commit: `docs(video): align demonstration with submitted map`.

## Task closeout

- Run the complete offline repository gate without provider, model, Supabase,
  BioMiner, or Flickr calls.
- Record exact tests, disabled GitHits, task SHA, remote SHA, remaining human
  recording/publication work, and the next safe action.
- Push `main` without force and verify the exact remote SHA.

Commit: `docs(provenance): close submitted video packet refresh`.
