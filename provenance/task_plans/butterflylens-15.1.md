# ButterflyLens Task 15.1 plan

Task: Unit and contract tests.

Commit: `test: cover ButterflyLens contracts`

## Scope

- Inventory every governed semantic fingerprint kind, JSON Schema, versioned
  policy, and submitted/live projection surface in the tracked repository.
- Exercise every current and legacy fingerprint kind and parent relationship
  with positive validation and digest/vocabulary mutation rejection.
- Require each schema group to remain structurally valid and reachable from
  named positive and negative contract evidence.
- Require every discovered policy version and submitted/projection export to
  have named positive and negative tests.
- Check projection fingerprint fields and privacy/scientific boundaries
  without calling a provider, model, or live service.

## Verification

- Focused inventory-drift, fingerprint vocabulary, schema graph, policy, and
  projection tests.
- A stdlib trace audit of the pre-task full Python suite as supporting line
  execution evidence, without treating line coverage as semantic proof.
- Full Python, contract-parity, web, Edge, rights, licensing, provenance, and
  staged safety gates.
- Exact commit, non-force `main` push, exact-SHA Pages and served-content check.

## Boundaries

This is local, deterministic test work. Current external facts and open-source
implementation search are not needed; Valyu is logged not needed and GitHits
remains disabled. BioMiner is active on dynamic pooling and still supplies no
immutable live GBIF handoff, so no partial BioMiner or external Flickr-fetch
output is copied. No Flickr API, provider submission, YOLOE, or BioCLIP work
occurs.
