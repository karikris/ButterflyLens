# ButterflyLens Task 17.2 plan

Task: Create the judge guide.

Commit: `docs: create ButterflyLens judge guide`

Starting SHA: `8a68c430adc76a670b5729d2ef958863d42bbafe`

## Objective and judging criterion

Provide one resettable, credential-free 90-second path through the eight exact
competition steps, plus a technical verification route, exact expected states
and counts, Submitted-versus-Live explanation, current limitations, rights and
provenance, and an optional live-worker path.

## Files and contracts

- Add `JUDGE_GUIDE.md` with contiguous timings that total exactly 90 seconds.
- Link the guide from the README competition hero.
- Add executable checks for the eight actions, timing, public anchors, exact
  stored analyst questions, snapshot-derived values, unavailable boundaries,
  supporting sections, and reachable local policy/report links.
- Record the Task 17.1 commit/push/Pages receipt and Task 17.2 provenance.

## Scientific, privacy, and live-state boundary

- The requested ALA/Flickr comparison, coverage-gap cell, community update, and
  live-M5 inspection are retained as route steps, but their current expected
  results are withheld, local-draft-only, or unavailable rather than fabricated.
- The public map has no selectable cell because occurrence and cell counts are
  rights-withheld. The guide treats that fail-closed state as evidence and never
  calls it absence.
- The review demonstrates a local blind draft; no stored event, contributor
  metric, consensus, or occurrence update is claimed.
- The GPT-5.6 step uses an exact stored question and labels the Submitted result
  model-free. The optional worker route stops unless the UI proves a fresh
  authenticated heartbeat, identity, committed artifact, and model receipts.
- The active external Flickr fetch and incomplete BioMiner work remain excluded.
  No Flickr, provider, live GPT, YOLOE, or BioCLIP call is made. GitHits remains
  disabled and is not called; no current external fact makes Valyu necessary.

## Verification

- Focused guide structure, timing, anchor, evidence, state, and link tests.
- Full Python, web, browser, Deno, parity, rights, licensing, security,
  generated-file, large-file, secret, and whitespace gates.
- Exact commit, push, remote SHA, Pages deployment, and public guide-link check.
